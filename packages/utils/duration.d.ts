export declare function humanDuration(ms: number, opts?: { max?: number }): string;
export declare function refreshCadence(ms: number): string;
export declare function relativeTime(then: number, now: number): string;
export default humanDuration;

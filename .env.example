# Copy to .env (git-ignored) or set in your Claude Code environment settings.
# READ-ONLY, short-lived Figma personal access token for the optional Figma
# bridge (references/figma-bridge.md). Never commit the real value.
FIGMA_TOKEN=
